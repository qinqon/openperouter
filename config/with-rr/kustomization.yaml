apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: openperouter-system

# Extends the default deployment with the internal iBGP Route Reflector controller.
# Use: make deploy-with-rr
# or:  kustomize build config/with-rr | kubectl apply -f -
resources:
- ../default
- ../rr-controller
